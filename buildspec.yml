version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 19.8.1
    commands:

  pre_build:
    commands:
  build:
    commands:

artifacts:
  files:
    - '**/*'
  base-directory: 'dist'

cache:
  paths:
    - node_modules/
